#include <iostream>
#include <map>
#include <windows.h>
#include <fstream>
using namespace std;

int main()
{
    SetConsoleOutputCP(CP_UTF8);
    map<int, string> dict;
    vector<int>keys;
    ifstream in("test.txt");
    if (in.is_open()) {
      }
    return 0;
}
